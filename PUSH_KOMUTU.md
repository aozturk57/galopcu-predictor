# GitHub'a Yükleme Komutu

GitHub'da repo oluşturduktan ve token aldıktan sonra, terminal'de şu komutu çalıştır:

```bash
cd "/Users/ardaozturk/galopcu_predictor web calisan 7 model kopyası 2"
git push -u origin main
```

**Username:** `aozturk57`
**Password:** Token'ı yapıştır (şifre değil!)

---

## Token Oluşturma:

1. https://github.com/settings/tokens
2. "Generate new token" → "Generate new token (classic)"
3. Note: `galopcu-predictor`
4. Expiration: 90 days
5. Scopes: `repo` işaretle
6. "Generate token"
7. Token'ı kopyala

